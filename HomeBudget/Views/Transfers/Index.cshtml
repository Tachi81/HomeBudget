@model IEnumerable<HomeBudget.Models.Transfer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AmountTransferred)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourceBankAccountsId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TargetBankAccountsId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Note)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AmountTransferred)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceBankAccountsId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TargetBankAccountsId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Note)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
